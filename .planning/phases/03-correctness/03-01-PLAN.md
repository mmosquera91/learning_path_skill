---
phase: 03-correctness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - subskills/eval.md
  - templates/syllabus.md
autonomous: true
requirements:
  - FIX-01
  - FIX-02
must_haves:
  truths:
    - "eval.md evaluation output uses consistent Mustache placeholder syntax"
    - "syllabus template references correct command format (/tutor confirm, /tutor edit)"
  artifacts:
    - path: "subskills/eval.md"
      provides: "Evaluation output template with consistent placeholder syntax"
      contains: "{{date}}"
      min_lines: 86
    - path: "templates/syllabus.md"
      provides: "Syllabus rendering template with correct command references"
      contains: "/tutor confirm"
---

<objective>
Fix template placeholder syntax in eval.md (FIX-01) and correct command format references in syllabus.md (FIX-02). Both fixes are small, targeted Markdown edits that address correctness bugs identified during phase research.
</objective>

<context>
@subskills/eval.md
@templates/syllabus.md
@.planning/phases/03-correctness/03-RESEARCH.md

## FIX-01 Analysis (from RESEARCH.md)
- eval.md step 6 output template should use Mustache `{{variable}}` consistently
- Current state: step 6 uses `{{date}}`, `{{score}}`, `{{feedback}}`, `{{#completed}}` — appears correct
- Research note: FIX-01 is MEDIUM confidence — verify actual file state during execution

## FIX-02 Analysis (from RESEARCH.md)
- templates/syllabus.md lines 30-31 use `/confirm` and `/edit` — WRONG
- SKILL.md and init.md confirm correct format is `/tutor confirm` and `/tutor edit`
- Confirmed bug: HIGH confidence
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix eval.md Mustache placeholder syntax (FIX-01)</name>
  <files>subskills/eval.md</files>
  <read_first>subskills/eval.md</read_first>
  <action>
## FIX-01: eval.md Mustache Placeholder Consistency

**Background:** REQUIREMENTS.md FIX-01 states: "eval.md uses plain text placeholders ({variable}) instead of Mustache syntax ({{variable}})"

**Verification (do first):** Read eval.md and check step 6 output template (lines 72-86). The template should use:
- `{{date}}` (not `{date}`)
- `{{score}}` (not `{score}`)
- `{{feedback}}` (not `{feedback}`)
- `{{#completed}}...{{/completed}}` sections (not `{completed}`)

If step 6 already uses Mustache syntax correctly, no change is needed — the bug may already be fixed or was a false positive in requirements.

If any plain text placeholders exist in step 6, replace them:
- `{date}` → `{{date}}`
- `{score}` → `{{score}}`
- `{feedback}` → `{{feedback}}`

**Important:** Do NOT modify step 2 (the LLM evaluation prompt). Those are plain text variables `{task_content}`, `{user_response}`, `{module_title}` that are intentional LLM prompt parameters, not Mustache template variables.
  </action>
  <acceptance_criteria>
    - subskills/eval.md step 6 output template uses `{{variable}}` Mustache syntax for all placeholders
    - step 6 contains `{{date}}`, `{{score}}`, `{{feedback}}`
    - step 6 uses `{{#completed}}...{{/completed}}` and `{{^completed}}...{{/completed}}` sections
    - step 2 LLM prompt variables (`{task_content}`, `{user_response}`, `{module_title}`) remain unchanged
    - grep "subskills/eval.md" -E "\{date\}|\{score\}|\{feedback\}" in step 6 section returns no matches
  </acceptance_criteria>
  <verify>
    <automated>grep -n "{{date}}\|{{score}}\|{{feedback}}" /home/server/.hermes/skills/tutor/subskills/eval.md</automated>
  </verify>
  <done>eval.md step 6 uses consistent Mustache placeholder syntax throughout</done>
</task>

<task type="auto">
  <name>Task 2: Fix syllabus.md command format references (FIX-02)</name>
  <files>templates/syllabus.md</files>
  <read_first>templates/syllabus.md</read_first>
  <action>
## FIX-02: Update syllabus.md Command References

**Bug:** templates/syllabus.md lines 30-31 reference `/confirm` and `/edit` which are rejected by Hermes v0.7+.

**Current lines (WRONG):**
```
- `/confirm` para activar este plan de aprendizaje
- `/edit <comentarios>` para solicitar cambios
```

**Required fix:**
Replace lines 30-31 with the correct command format:
```
- `/tutor confirm` para activar este plan de aprendizaje
- `/tutor edit <comentarios>` para solicitar cambios
```

This matches the command format used in:
- SKILL.md lines 56-57 (`/tutor confirm`, `/tutor edit <feedback>`)
- init.md lines 96-97 (`/tutor confirm`, `/tutor edit <feedback>`)
- README.md line 31 (`/tutor confirm`, `/tutor edit [feedback]`)
  </action>
  <acceptance_criteria>
    - templates/syllabus.md line 30 contains `/tutor confirm` (not `/confirm`)
    - templates/syllabus.md line 31 contains `/tutor edit` (not `/edit`)
    - templates/syllabus.md does NOT contain the text `/confirm` or `/edit` (without `/tutor` prefix)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "/tutor confirm\|/tutor edit" /home/server/.hermes/skills/tutor/templates/syllabus.md && ! grep -n "^[^#]* /confirm\|^[^#]* /edit" /home/server/.hermes/skills/tutor/templates/syllabus.md</automated>
  </verify>
  <done>syllabus.md references `/tutor confirm` and `/tutor edit` — matching SKILL.md and init.md</done>
</task>

</tasks>

<verification>
## Phase 3 Correctness Verification

After both plans execute:
1. `grep "{{date}}|{{score}}|{{feedback}}" subskills/eval.md` — step 6 uses Mustache
2. `grep "/tutor confirm|/tutor edit" templates/syllabus.md` — correct command format
3. `grep -v "#" templates/syllabus.md | grep "/confirm\|/edit"` — returns nothing
</verification>

<success_criteria>
- eval.md step 6 uses consistent `{{variable}}` Mustache syntax for all placeholders
- templates/syllabus.md references `/tutor confirm` and `/tutor edit` (not `/confirm` or `/edit`)
</success_criteria>

<output>
After completion, create `.planning/phases/03-correctness/03-01-SUMMARY.md`
</output>
