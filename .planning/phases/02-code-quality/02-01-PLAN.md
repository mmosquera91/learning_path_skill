---
phase: 02-code-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - subskills/daily.md
autonomous: true
requirements:
  - DEDUP-01
  - QUAL-02
user_setup: []

must_haves:
  truths:
    - "SKILL.md routes commands correctly with fewer than 200 lines"
    - "Tier rules are referenced from CONTRIBUTING.md, not duplicated inline"
    - "daily.md has the 8-line tier summary table inline for cron context. SKILL.md has a reference to CONTRIBUTING.md per D-09."
  artifacts:
    - path: SKILL.md
      provides: Command routing, persona, rules
      max_lines: 200
    - path: subskills/daily.md
      provides: Daily cron subskill with tier table
      min_lines: 96
  key_links:
    - from: SKILL.md
      to: CONTRIBUTING.md
      via: "See CONTRIBUTING.md §1-3 for full tier rules"
    - from: subskills/daily.md
      to: validate_urls.py
      via: "8-line tier table inline (for cron context self-containment)"
---

<objective>
Reduce SKILL.md to under 200 lines by deduplicating tier rules, removing topic-specific examples, and trimming PITFALLS. Add 8-line tier summary table to daily.md for cron context self-containment per D-09.
</objective>

<context>
@SKILL.md
@CONTRIBUTING.md (lines 1-16, canonical tier rules and 8-line summary table)
@subskills/daily.md

# Tier rule reference structure (D-07, D-09):
# - SKILL.md: short reference only (no inline table per D-09)
# - Subskills (daily.md): 8-line table inline (for cron context with zero prior context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deduplicate SKILL.md tier rules and reduce to under 200 lines</name>
  <files>SKILL.md</files>
  <action>
    Read SKILL.md and CONTRIBUTING.md before editing.

    SKILL.md currently has:
    - Lines 42-66: ~25-line tier rules block (TIER SYSTEM table + rules + topic examples)
    - Lines 59-65: Topic-specific TIER 1 examples (~7 lines)
    - PITFALLS section (~10 lines, lines 33-41)

    Apply ALL of the following changes to SKILL.md:

    1. REMOVE lines 59-65 (Topic-Specific TIER 1 Examples section entirely)

    2. REPLACE the tier rules block (current lines 42-66) with this shorter version (~16 lines total):
    ```
    ## SOURCE TIER SYSTEM (URL Reliability)

    See CONTRIBUTING.md §1-3 for full tier rules and topic-specific examples.

    **RULES:**
    - MINIMUM 50% of resources MUST be TIER 1 (interactive platforms)
    - NO YouTube playlist URLs (they break when videos are reordered)
    - VERIFY all TIER 1 URLs match known valid patterns before presenting
    - If validation fails for >30% of URLs, regenerate with more conservative sources
    ```

    NOTE: SKILL.md does NOT have an inline tier table per D-09 — it uses direct reference only. The verification checks for 0 inline tier rows.

    3. TRIM PITFALLS section from ~10 lines to ~6 lines by removing the Hermes v0.7 workaround bullet (line 40) — keep only the most critical pitfalls that are still relevant:
       - Keep: Syllabus generation research requirement, URL specificity, SQLite save from execute_code warning
       - Remove or shorten: Research delegation explanation, Hermes workaround

    After all changes, SKILL.md must be under 200 lines. Verify with: `wc -l SKILL.md`
  </action>
  <verify>
    <automated>grep -c "TIER 1\|TIER 2\|TIER 3\|TIER 4" SKILL.md | awk '{if ($1 <= 0) print "PASS"; else print "FAIL: found " $1 " tier lines (should be 0 - reference only per D-09)"}' && wc -l SKILL.md | awk '{if ($1 < 200) print "PASS: " $1 " lines"; else print "FAIL: " $1 " lines (must be < 200)"}'</automated>
  </verify>
  <done>
    SKILL.md under 200 lines. Tier rules block replaced with reference only (no inline table per D-09). Topic-specific examples removed. PITFALLS trimmed. grep confirms 0 inline tier rows (reference only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 8-line tier summary table to daily.md for cron self-containment</name>
  <files>subskills/daily.md</files>
  <action>
    Read subskills/daily.md before editing.

    Per D-09: Subskills that run in cron (daily.md) need the 8-line tier summary table inline since cron sessions have zero context and cannot reference CONTRIBUTING.md.

    Find the appropriate insertion point in daily.md — insert the 8-line tier summary table near the top of the file (after the trigger section or at the beginning of the steps), where it provides context for resource review without being intrusive.

    Use this exact 8-line table from CONTRIBUTING.md lines 9-14:
    ```
    | Tier | Source Type | Examples | Reliability | Max/Module |
    |------|-------------|----------|-------------|------------|
    | TIER 1 | Interactive platforms | exercism.org, codecademy.com, duolingo.com, chess.com/lessons | ⭐⭐⭐⭐⭐ | Unlimited |
    | TIER 2 | Official courses | Coursera, edX, Khan Academy, docs | ⭐⭐⭐⭐ | 2 |
    | TIER 3 | YouTube (single videos ONLY) | Individual videos, NO playlists | ⭐⭐ | 1 |
    | TIER 4 | Reference materials | Wikipedia, technical blogs | ⭐⭐ | 1 |
    ```

    The table should be preceded by a brief header like:
    "## Resource Tier Reference (for cron context)"

    daily.md line count will increase by ~10 lines (8 table + 2 header lines). This is intentional per D-09.
  </action>
  <verify>
    <automated>grep -c "TIER 1\|TIER 2\|TIER 3\|TIER 4" subskills/daily.md | awk '{if ($1 >= 4) print "PASS: tier table present"; else print "FAIL: tier table missing"}' && wc -l subskills/daily.md | awk '{print "Line count: " $1}'</automated>
  </verify>
  <done>
    daily.md contains the 8-line tier summary table inline. daily.md is self-contained for cron execution without requiring reference to CONTRIBUTING.md.
  </done>
</task>

</tasks>

<verification>
- `wc -l SKILL.md` reports fewer than 200 lines
- `grep -c "TIER 1\|TIER 2\|TIER 3\|TIER 4" SKILL.md` returns 0 (reference only, no inline table per D-09)
- `grep "See CONTRIBUTING.md" SKILL.md` finds the reference
- `grep "TIER 1\|TIER 2\|TIER 3\|TIER 4" subskills/daily.md` finds the inline tier table
- Phase 1 tests still pass: `python3 -m pytest tests/ -x`
</verification>

<success_criteria>
SKILL.md has fewer than 200 lines and contains a reference to CONTRIBUTING.md for full tier rules (no inline tier table per D-09). daily.md has the 8-line tier summary table inline for cron self-containment. Command routing in SKILL.md is preserved (all router table entries intact).
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-01-SUMMARY.md`
</output>