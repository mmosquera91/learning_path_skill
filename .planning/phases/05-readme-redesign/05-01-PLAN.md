---
phase: 05-readme-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-01
  - README-02
user_setup: []
must_haves:
  truths:
    - README.md mentions only features that are actually implemented and tested
    - Setup instructions match actual hermes skills install workflow
    - Example session reflects actual /tutor init -> /tutor confirm -> /tutor submit -> /tutor eval flow
    - Command formats (/tutor confirm, /tutor edit, /tutor submit) match SKILL.md router
  artifacts:
    - path: README.md
      provides: User-facing documentation
      min_lines: 250
    - path: .planning/phases/05-readme-redesign/05-AUDIT.md
      provides: Discrepancy report documenting all issues found
      min_lines: 50
  key_links:
    - from: README.md
      to: SKILL.md
      via: Command format references
      pattern: /tutor (init|confirm|edit|submit|status|skip|pause|resume|review|switch|export)
    - from: README.md
      to: subskills/eval.md
      via: Evaluation rubric description
      pattern: Score: 1-10
---

<objective>
Audit the current README.md against actual implementation and rewrite it to accurately reflect current state.

Purpose: Remove untested claims, fix setup instructions, add real example session, fix command formats.
Output: Corrected README.md + AUDIT.md discrepancy report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md (current README to audit and rewrite)
@SKILL.md (source of truth for commands, persona, router)
@subskills/init.md (init flow)
@subskills/daily.md (daily task flow)
@subskills/eval.md (evaluation prompt and trigger)
@subskills/adapt.md (adaptation flow)
@.planning/phases/05-readme-redesign/05-RESEARCH.md (prior research)
@.planning/REQUIREMENTS.md (requirements README-01, README-02)

## Key Implementation Facts (verified from source files)

### Correct GitHub URL
git remote shows: `https://github.com/mmosquera91/learning_path_skill.git`
The README URL is CORRECT. Skill directory is `tutor/` (auto-registers as /tutor) but repo name is `learning_path_skill`.

### eval.md Trigger Issue
eval.md line 4 says: `/submit <response>`
SKILL.md router says: `/tutor submit <response>` → eval.md
This is a DISCREPANCY. The README should use `/tutor submit` (matching the router), not `/submit`.

### Evaluation Rubric (eval.md step 2)
Prompt asks for: "Score: 1-10" (single score, NOT two-axis)
JSON schema in AGENTS.md expects: `conceptual_comprehension` + `application_ability`
The README's two-axis description may not match what the system actually prompts for.

### Decision Rules (eval.md step 4-5)
- score >= 7.0: module completed
- score 4.0-6.9: module in_progress (repeat tomorrow)
- score < 4.0: decompose (not explicitly documented in README)
README shows: Average >= 7.0 advance, 4.0-6.9 repeat, < 4.0 decompose — matches eval.md

### Spaced Repetition Rules (eval.md step 6)
- score >= 8.0: review in 7 days
- score 5.0-7.9: review in 3 days
- score < 5.0: review next session
README shows: >= 8.0 in 7 days, 5-7.9 in 3 days, < 5 next day — matches eval.md

### Cron Creation
init.md step 8: cron jobs created during `/tutor confirm` via `cronjob(action="create")`
README correctly describes automatic cron creation.

### /tutor export
SKILL.md inline command (no subskill) — confirms Obsidian export exists.
README correctly documents this.

### Inactivity Handling
daily.md step 2+: 0-1 days normal, 2 days nudge+skip, 3 days pause offer, 5+ auto-pause.
README correctly documents this.

## Discrepancies to Fix

| # | Location | Current | Should Be |
|---|----------|---------|-----------|
| D1 | eval.md trigger | `/submit <response>` | `/tutor submit <response>` (router mismatch) |
| D2 | README Setup, Cron Jobs | `/confirm` | `/tutor confirm` |
| D3 | README Limitations | `/confirm` step | `/tutor confirm` step |
| D4 | README Evaluation Rubric | "two-axis: Conceptual Comprehension + Application Ability" | "Score: 1-10 (single score, model outputs structured feedback)" |
| D5 | README Example session | "Score: 4.5/10" with conceptual/application breakdown | Single averaged score format (e.g., "Score: 6/10") |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit README.md against source files</name>
  <files>.planning/phases/05-readme-redesign/05-AUDIT.md</files>
  <read_first>
README.md (file being audited)
SKILL.md (router, commands)
subskills/eval.md (evaluation prompt)
</read_first>
  <action>
Create a detailed audit report at .planning/phases/05-readme-redesign/05-AUDIT.md documenting every discrepancy between README.md and the actual implementation.

For each discrepancy, document:
- Line/section in README
- What README says
- What the source file says
- Source file and line reference

Cover at minimum:
1. Command formats (/tutor vs /confirm inconsistencies)
2. GitHub URL accuracy (verify against git remote)
3. Evaluation rubric description vs eval.md step 2
4. Example session score format
5. Features claimed but not implemented
6. Features implemented but not claimed
7. Setup/installation workflow accuracy

Also note the eval.md trigger discrepancy (/submit vs /tutor submit) as a CODE BUG, not just a documentation issue.
</action>
  <verify>
File .planning/phases/05-readme-redesign/05-AUDIT.md exists and contains at minimum:
- git remote verification (https://github.com/mmosquera91/learning_path_skill.git)
- eval.md trigger discrepancy documented
- /confirm vs /tutor confirm corrections listed
- Evaluation rubric correction documented
</verify>
  <done>AUDIT.md exists with all discrepancies documented, each with source file references</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md with all corrections</name>
  <files>README.md</files>
  <read_first>
README.md (current state)
SKILL.md (commands, router, persona)
subskills/eval.md (evaluation flow, scoring rules)
subskills/init.md (init flow, cron creation)
subskills/daily.md (daily task, inactivity handling)
subskills/adapt.md (weekly review)
05-RESEARCH.md (research findings)
05-AUDIT.md (audit completed in Task 1)
</read_first>
  <action>
Rewrite README.md completely. Use the audit findings to guide corrections.

## Sections to INCLUDE:
1. **What It Does** — Keep accurate features list. Mark spaced repetition as "implemented but not yet validated end-to-end" (keep this accurate).
2. **Example Session** — Fix to show:
   - `/tutor init Python` (correct command)
   - `/tutor confirm` (NOT /confirm)
   - `/tutor submit <response>` (NOT bare /submit)
   - Score: single averaged number (e.g., "6/10") — NOT two-axis breakdown
   - Decision: repeat/advance message
   - Make it realistic, not polished
3. **Architecture** — Keep as-is (file structure matches)
4. **Commands table** — Verify all formats are `/tutor <subcommand>` — fix any inconsistencies
5. **Setup/Prerequisites** — Keep accurate (Hermes, Telegram, Python 3.11+, curl)
6. **Installation** — Use correct git URL: `https://github.com/mmosquera91/learning_path_skill.git` (verified against git remote)
7. **Cron Jobs** — Fix `/confirm` to `/tutor confirm` in description
8. **Evaluation Rubric** — Describe single 1-10 score, NOT two-axis. Decision rules match eval.md (>= 7.0 advance, 4.0-6.9 repeat, < 4.0 decompose). Spaced repetition rules match eval.md step 6.
9. **Inactivity Handling** — Keep as-is (matches daily.md)
10. **SQLite Schema** — Keep brief (6 tables, init_db.py reference)
11. **Design Decisions** — Keep table, verify accuracy
12. **Limitations** — Fix `/confirm` to `/tutor confirm`. Keep accurate items.
13. **Roadmap** — Keep v1.0 features accurate. Keep v1.1 in-progress items.
14. **License** — Keep MIT

## Key corrections to make:
- Line 31: "Reply /tutor confirm to activate" — ALREADY CORRECT
- Line 51: "Reply /tutor submit <your answer>" — ALREADY CORRECT
- Line 53: "/tutor submit" — ALREADY CORRECT
- Line 65: Score 4.5/10 with breakdown — CHANGE to single score (e.g., "6/10") matching eval.md
- Line 148: "/confirm" — FIX to `/tutor confirm`
- Line 169-175: Two-axis rubric description — REWRITE to single 1-10 score
- Line 227: "/confirm step" — FIX to `/tutor confirm`

## What to KEEP accurate (no changes needed):
- GitHub URL (verified correct via git remote)
- Spaced repetition "not yet validated" caveat
- /tutor status, /tutor skip, /tutor pause, /tutor resume, /tutor review, /tutor switch, /tutor export commands
- Architecture diagram
- Design decisions table
</action>
  <verify>
README.md passes these checks:
1. grep -c "/tutor confirm" README.md >= 1 (command format correct)
2. grep -c "/tutor init" README.md >= 1 (command format correct)
3. grep -c "/tutor submit" README.md >= 1 (command format correct)
4. grep -c "/confirm" README.md == 0 (no bare /confirm commands)
5. grep "Score:" README.md shows single 1-10 format, NOT two-axis
6. grep "learning_path_skill" README.md shows correct URL
</verify>
  <done>README.md accurately reflects current implementation, all command formats match SKILL.md, evaluation rubric matches eval.md, setup instructions are accurate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — Documentation phase, no code changes, no external service interactions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation | README installation instructions | mitigate | Verify git remote URL matches README (confirmed: `https://github.com/mmosquera91/learning_path_skill.git` matches README) |
| T-05-02 | Information | False feature claims in README | mitigate | Audit found no features claimed that don't exist. README accurately describes v1.0 features. |
| T-05-03 | Repudiation | Misleading example session | mitigate | Rewrite example to show single averaged score format matching eval.md output |
</threat_model>

<verification>
1. README.md exists at project root with minimum 250 lines
2. All command references use `/tutor <subcommand>` format (no bare `/confirm`, `/submit`)
3. Evaluation rubric describes single 1-10 score matching eval.md
4. Example session shows realistic flow with correct commands
5. GitHub URL matches verified git remote
6. AUDIT.md exists with discrepancy documentation
</verification>

<success_criteria>
- README.md mentions ONLY features that are actually implemented and tested
- Setup instructions match actual `hermes skills install git+https://github.com/mmosquera91/learning_path_skill.git` workflow
- Example session reflects actual `/tutor init` -> `/tutor confirm` -> `/tutor submit` -> eval flow
- Command formats (e.g., `/tutor confirm`, `/tutor edit`) match actual SKILL.md router
- Evaluation rubric describes single 1-10 score (not two-axis) matching eval.md step 2
- Spaced repetition flagged as "not yet validated end-to-end" (kept accurate)
- AUDIT.md documents all discrepancies found
</success_criteria>

<output>
After completion, create `.planning/phases/05-readme-redesign/05-01-SUMMARY.md`
</output>
