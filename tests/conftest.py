"""Shared test fixtures for the Tutor skill test suite."""
import sys
import os

# Add scripts/ to path so we can import modules
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
